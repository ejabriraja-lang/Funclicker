name: Super Hammer Execution
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * *' # يعمل تلقائياً كل يوم في منتصف الليل

jobs:
  run-hammer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install googleapis firebase-admin

      - name: Run Super Hammer
        env:
          GOOGLE_KEY: ${{ secrets.GOOGLE_KEY }}
          FIREBASE_KEY: ${{ secrets.FIREBASE_KEY }}
        run: node index.js

      - name: Commit Sitemap Change
        run: |
          git config --local user.name 'Sitemap Bot'
          git config --local user.email 'bot@funclickergame.com'
          git add public/sitemap.xml
          git commit -m "🤖 تحديث الخريطة تلقائياً" || echo "No changes"
          git push
